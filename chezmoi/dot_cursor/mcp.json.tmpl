{{- /*
  Cursor MCP configuration template
  MCP servers are injected from chezmoidata.json via .cursor_config
*/ -}}
{{- $data := include (joinPath .chezmoi.homeDir ".config/chezmoi/chezmoidata.json") | fromJson -}}
{{ $data.cursor_config | toPrettyJson }}
